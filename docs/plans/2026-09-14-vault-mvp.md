# Vault MVP: web first, Telegram next

User steering, 14 September 2026: remove the 30% weekly usage floor entirely. Prioritize a basic usable MVP: a distinctive landing, real web practice and Telegram if the existing engine makes it straightforward. Docker was moved to another disk; diagnose it separately and do not let confidential-compute preparation delay this MVP. This supersedes the old quota text in the active goal and prior plans.

The funded release remains separate pending actual token, inference qualification, prize authority and transaction approvals. Do not label practice as a funded bounty or call simulated replies real AI.

## Accepted implementation direction

- Existing Node/vanilla frontend, English copy, the silver custodian and its dry humor. Warm ivory/ink with a restrained orange accent. Large editorial typography, character-led hero and a clear invitation to play; no generic feature-card grid or invented statistics.
- Landing at `/`; playable chat at `/play`. Preserve earlier `/?ui=...` links to the chat. The game continues to fit one screen; the landing may have a short explanation below its hero.
- Reuse the actual rules and model service. Keep costs bounded and public practice authenticated. The user approved the dedicated Vault MVP key and up to USD 5 inference, then added USD 10 account credit. Nine supervised local requests produced eight valid replies and one 429; key usage reconciles to USD 0.00595465. Public startup and the automated runner still reject its missing BYOK-in-limit flag; keep that check intact.
- Telegram is private-chat practice through a bot, not the user's personal account: a small long-polling adapter in the web server process reuses the game service, persistent usage limits, owner-separated sessions and update deduplication. No wallet, public group behavior or funded gameplay in that adapter. Bot credentials stay in `.env`.
- Prepare the complete local result before any public deployment or provider spend approval. Do not use Docker or an attestation claim as a requirement for practice.

## Work

- [x] Implement the landing and `/play` entry, with clear practice status, working navigation and responsive states.
- [x] Simplify first-use chat navigation and preserve rules/receipts/mascot behavior.
- [x] Build and test the small Telegram adapter, crash/deduplication behavior and bounded provider usage; document the bot setup needed for a live test.
- [ ] Verify web behavior and rendering where the browser permits, integration behavior with controlled transports, and actual provider calls only after credentials/budget are configured.
- [x] Prepare an exact public deployment option and handoff with remaining access requirements.

Browser limitation: Brave still reports ERR_BLOCKED_BY_CLIENT for `http://127.0.0.1:4319/`. No protection or browser was changed to bypass it. Native browser UI automation is restricted to CUA. Local HTTP tests and source inspection remain available; this does not count as current visual QA.


Implementation evidence: 200 tests and build passed after the landing/router, account access and Telegram adapter changes. The concurrency fixture now supplies both the landing and chat documents. Live HTTP returned 200 for the new pages and assets. Real inference, Privy login, Telegram identity/delivery and public deployment are pending access, budget or user input. See `docs/treasure-guardian/MVP-WEB-TELEGRAM.md` and `TELEGRAM-MVP.md`. Docker settings, VHDX and WSL registration point to E:; Desktop/backend were not running in the last read-only check. No storage was changed.

Continuation audit, 23:24 UTC: previous turn was progress (landing/game/Telegram source and 200-test release). This turn was progress: a 144-file deployment package was extracted, hash-checked and booted from a separate temporary directory after an offline dependency install; eight routes passed and a no-key attempt was rejected before inference. Temporary test directories were removed. Report: `vault-lab/output/mvp-package.json`. The access blocker is unchanged for a second consecutive goal turn: no API key/budget answer, bot token, real Privy login evidence or public host/origin. Do not repeat completed tests or invent additional product scope merely to stay active; the next useful step is live activation once those inputs arrive. Browser visual QA also remains blocked by Brave's localhost page.

Resumed activation, 15 September 00:05 UTC: the user approved Vault MVP key creation and up to USD 5 inference. Key authentication, total non-resetting limit and local runtime configuration verified. Account balance USD 0, no inference or credit purchase. An unexpected onboarding-generated unlimited key was disabled and its Enable menu verified; the older existing key was untouched. The current editor exposes no include_byok_in_limit toggle and the documented update API requires a management key. No new administrative access was created, and public-mode budget validation remains unchanged. Report: `vault-lab/output/openrouter-setup-check.json`. This is progress after the prior blocked goal, not another unchanged blocked turn.
