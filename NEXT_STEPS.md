# Handoff — 2026-09-10

## Current priority — supersedes the older route proposals below

User chose Google as the pair; prepare GOOGL on Robinhood via Long, with Pons as the alternative. Freeze further design/social scope until a real launch route is verified. See `integrations/long/README.md` for commands and exact evidence.

The isolated unsigned preparer is implemented. The GOOGL diagnostic transaction reverted with EnforcedPause(); the reviewed Long ticker factory returned paused=true at block 59696393 (2026-09-10 20:40 UTC). Current frontend bundles differ from the captured OCAT baseline, so investigate current factory/encoding before claiming all Long launches are paused. The oracle was also older than the preparer's one-hour freshness policy. Encoding parity passed; EVM success and gas estimate did not. No signer, broadcast or neural binding is implemented.

GOOGL is selected for preparation; BELLFLY identity, public creator/fee recipient, final metadata/economics and budget are still outstanding. Existing neural artifacts retain frozen Bankr/NVDA proposals and are not authorization for this new route.

## Postlaunch direction under discussion

See `docs/POST_LAUNCH_AND_X.md` for the user's questions about meaningful differentiation, ongoing activity and letting the fly operate X. A neural LAUNCH/NO_LAUNCH gate is methodological credibility, not a proven market advantage. Proposed follow-on: an interactive character with a small observable action space and X as a diary via the official API. Current runner is episodic; continuity, social decoder and outbox are not implemented. This is a proposal, not an approved pivot or authorization to connect/post/pay.

## New competitor evidence changes integration research priority

The user identified flybrain.online. FLYBRAIN creation through the exact Pons V2 factory is confirmed in Blockscout (2026-09-10 18:23:09 UTC). See `docs/competitor-flybrain/REVIEW.md` and `competidores.json`. Re-evaluate Pons preview/operator/quote eligibility alongside Bankr before committing to the first provider integration. The site's general fly-launches-token narrative is already occupied; do not present BELLFLY's theme alone as novel. Their published rig programmatically launches/confirms after assisted neural form completion; exact neural LAUNCH/NO_LAUNCH causality remains a distinct question.

## Latest product update

The web now has a memecoin identity inspired by our OCAT site, explicitly requested by the user: electric green, oversized BELLFLY wordmark, framed original fly mascot, “Let the fly ring the bell.” hero, Proof of buzz and a dated launch-gap section. See `docs/BRAND_DIRECTION.md` and the concrete Spanish handoff `docs/LAUNCH_GAP.md`. The next engineering milestone is an official Bankr preview with strict no-broadcast semantics; the existing controller still cannot launch. No original evidence/core pins were changed by the redesign.

## Delivered locally

- React/Vite English lab, backed by actual recorded model events, with playback, controls, evidence download and explicit provenance.
- Original pinned Shiu/FlyWire v630 model running on CPU through isolated child transport; modern locked Python environment. Real positive, quiet negative and verified MN9-input ablation controls, each replayed successfully.
- Persistent state machine, frozen manifests, canonical hashes, full exports, verifier and credential-free local replay.
- Durable offline adapter with atomic reservation, restart recovery and ambiguous-result handling. Live execution is impossible in this implementation.
- Fifteen passing TypeScript integration/security tests and one passing actual Brian2 ablation regression test; build succeeds. See `docs/VALIDATION.md`.
- Dated source and competitor records, public protocol proposal, future issuance checklist, English value proposition and private X-thread draft. Grok was consulted privately in Brave as requested; its unsupported claims were not treated as evidence.

The workspace started as an empty, unborn Git repository with two existing Ramborghini images; those images were preserved. Files are local and uncommitted. No remote, PR, publication, wallet connection or transaction was created.

## Immediate next action

Open `http://127.0.0.1:8787/?run=12a959cf-4a7e-48a9-9547-90d3c9d3a41e` while `npm.cmd run dev` is running. Inspect the positive, 0 Hz and ablated records in the selector and replay the named examples using README commands. No decision or financial access is needed for this completed local path.

Before calling the neural policy validated, define a scientific calibration protocol independently of a desired LAUNCH result: compare the modern implementation against the published reference, justify the MN9/threshold interpretation, and preregister additional conditions/seeds and exclusion criteria. Keep all outcomes. Do not silently replace this demonstration with a selected favorable run.

## Integration work still pending

1. Keep Robinhood Chain 4663 + Bankr as the proposed route. Chain connectivity and NVDA registry identity were verified. Exact NVDA quote compatibility, executable liquidity, account eligibility, current economics, signer permissions and official simulation were not. `preview` currently means our offline adapter, not Bankr acceptance.
2. Build an official no-broadcast preview only with verified payload semantics and an eligible operator account, through a separately authorized integration. No existing financial credentials were read. Pons v2 is a separate possible integration with public-launch/whitelist and deployment-verification questions, not a drop-in replacement.
3. Implement and verify the external prior timestamp/public randomness protocol before any official run. The proposed protocol is in `docs/PUBLIC_RUN_PROTOCOL.md`; all current commitments are local and seeds fixed.
4. Before adding live execution, implement actual provider receipt/configuration verification, least-privilege signing isolation and remote reconciliation. Do not infer remote idempotency from SQLite. Resolve ambiguous submissions without automatic redeploy.
5. Assess dataset terms for the actual intended public experiment, with attribution and rights as needed. The user described trying what the fly can do, without selling anything; do not assume that every such experiment is commercial or stop local research. Other teams' launches do not establish their permissions. MaleCNS is an alternative dataset with different scientific mapping work, not a silent substitution.

## Decisions that belong before a future real run

Name/ticker, exact provider and chain, verified quote, supply and all allocations, fees and beneficiaries, budget, randomness source, scientific rule and full manifest must be fixed first. The user's explicit approval must apply to that manifest and budget **before** the official run starts. No discretionary approval after seeing a positive signal may be presented as autonomous execution.

Until then: `live_execution_enabled=false`. The current experiment's meaningful result is the reproducible causal signal and the offline consequence. Do not claim a live token, financial reasoning, consciousness, a world first or independent proof of execution.
